import { Work_Sans, Besley } from 'next/font/google'

export const workSans = Work_Sans({
  subsets: ['latin'],
  display: 'swap',
})

export const besley = Besley({
  subsets: ['latin'],
  display: 'swap',
})
